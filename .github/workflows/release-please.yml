name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main
permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: BlueLua/.github/.github/workflows/release-please.yml@main
